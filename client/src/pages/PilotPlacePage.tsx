import { useState, type FormEvent } from "react";
import { CheckCircle2, ChevronLeft, ExternalLink, MapPinned, MessageSquare, Phone, Send, UtensilsCrossed } from "lucide-react";
import { Link, useRoute } from "wouter";
import { MvpContentState } from "@/components/MvpContentState";
import { getPilotCity, getPilotItems, loadPilotCatalog, type PilotCatalog } from "@/lib/mvpPilot";
import { trackMvpEvent } from "@/lib/mvpEvents";
import { useEffect } from "react";

function SourceLine({ name, url, verifiedAt }: { name: string; url: string; verifiedAt: string }) {
  return <p className="text-xs leading-5 text-[#67705D]">Fonte: <a href={url} target="_blank" rel="noopener noreferrer" className="font-bold text-[#566B37] underline decoration-[#566B37]/35 underline-offset-2">{name}</a> · {verifiedAt}</p>;
}

function PlaceFeedback({ placeTitle }: { placeTitle: string }) {
  const [submitted, setSubmitted] = useState(false);

  function handleSubmit(event: FormEvent<HTMLFormElement>) {
    event.preventDefault();
    setSubmitted(true);
  }

  return <section className="mx-auto mt-2 max-w-5xl px-4 pb-14 sm:px-6 lg:px-8" aria-labelledby="place-feedback-title"><div className="rounded-[2rem] border border-[#B9572D]/20 bg-[#FFF7E8] p-6 sm:p-8"><div className="grid gap-7 lg:grid-cols-[0.8fr_1.2fr] lg:items-center"><div><div className="inline-flex items-center gap-2 text-[10px] font-extrabold uppercase tracking-[0.15em] text-[#B9572D]"><MessageSquare className="h-4 w-4" aria-hidden="true" /> Sua leitura</div><h2 id="place-feedback-title" className="display-font mt-3 text-3xl tracking-[-0.05em]">O que você achou deste lugar?</h2><p className="mt-3 max-w-md text-sm leading-6 text-[#66705E]">Conte se as informações desta página foram úteis ou se faltou algum contexto sobre {placeTitle}.</p><p className="mt-4 text-xs font-semibold leading-5 text-[#566B37]">Não inclua nome, e-mail ou outros dados pessoais.</p></div>{submitted ? <div className="rounded-[1.5rem] bg-[#566B37] p-6 text-white" role="status" aria-live="polite"><CheckCircle2 className="h-8 w-8" aria-hidden="true" /><p className="mt-4 text-lg font-extrabold">Obrigado pelo retorno.</p><p className="mt-1 text-sm leading-6 text-white/80">Seu feedback foi registrado nesta demonstração.</p><button type="button" onClick={() => setSubmitted(false)} className="tap mt-5 rounded-full bg-white px-4 py-2.5 text-sm font-extrabold text-[#3C482D]">Enviar outro</button></div> : <form onSubmit={handleSubmit} className="space-y-4"><label htmlFor="place-feedback-message" className="text-sm font-extrabold text-[#3C482D]">Seu feedback</label><textarea id="place-feedback-message" name="message" required minLength={10} rows={5} placeholder="Escreva uma observação sobre esta página..." className="mt-1.5 w-full resize-y rounded-2xl border border-[#3C482D]/15 bg-[#FFFDF6] px-4 py-3 text-sm leading-6 text-[#2E3222] outline-none placeholder:text-[#7A806D] focus:border-[#B9572D] focus:ring-2 focus:ring-[#B9572D]/30" /><p className="text-xs text-[#7A806D]">Mínimo de 10 caracteres.</p><button type="submit" className="tap inline-flex items-center justify-center gap-2 rounded-full bg-[#B9572D] px-5 py-3 text-sm font-extrabold text-white hover:bg-[#A34827] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-[#B9572D] focus-visible:ring-offset-2">Enviar feedback <Send className="h-4 w-4" aria-hidden="true" /></button></form>}</div></div></section>;
}

export default function PilotPlacePage() {
  const [, params] = useRoute("/cidades/:citySlug/locais/:itemSlug");
  const [catalog, setCatalog] = useState<PilotCatalog | null>(null);

  useEffect(() => { loadPilotCatalog().then(setCatalog); }, []);

  if (!catalog) return <main className="min-h-screen bg-[#F5ECD8] px-4 py-16 sm:px-6"><div className="mx-auto max-w-2xl"><MvpContentState kind="loading" title="Preparando o lugar" description="Organizando contexto, fonte e ações de visita." /></div></main>;

  const city = getPilotCity(params?.citySlug ?? "", catalog);
  const item = city ? getPilotItems(city.slug, "all", "all", catalog).find((entry) => entry.slug === params?.itemSlug) ?? null : null;
  if (!city || !item) return <main className="min-h-screen bg-[#F5ECD8] px-4 py-16 sm:px-6"><div className="mx-auto max-w-2xl"><MvpContentState kind="error" title="Local fora do recorte atual" description="Este endereço não está entre os lugares publicados para as cidades-piloto." action={<Link href={city ? `/cidades/${city.slug}` : "/"} className="tap inline-flex items-center gap-2 rounded-full bg-[#3C482D] px-4 py-2.5 text-sm font-extrabold text-white"><ChevronLeft className="h-4 w-4" /> Voltar</Link>} /></div></main>;

  return <div className="min-h-screen bg-[#F5ECD8] text-[#2E3222]"><header className="border-b border-[#3C482D]/12 bg-[#F5ECD8]/95 backdrop-blur"><div className="mx-auto flex min-h-[72px] max-w-5xl items-center px-4 sm:px-6"><Link href={`/cidades/${city.slug}`} className="tap inline-flex items-center gap-2 text-sm font-extrabold text-[#566B37] hover:text-[#B9572D]"><ChevronLeft className="h-4 w-4" /> Voltar para {city.name}</Link></div></header><main><section className="px-4 py-12 sm:px-6 lg:px-8" style={{ background: `linear-gradient(135deg, ${city.accent} 0%, #3C482D 72%)` }}><div className="mx-auto max-w-5xl text-white"><p className="text-[10px] font-extrabold uppercase tracking-[0.15em] text-white/70">{city.name} · {item.category}</p><h1 className="display-font mt-4 max-w-3xl text-5xl leading-none tracking-[-0.06em] sm:text-6xl">{item.title}</h1><p className="mt-5 max-w-2xl text-base leading-7 text-white/80 sm:text-lg">{item.summary}</p></div></section><section className="px-4 py-12 sm:px-6 lg:px-8"><div className="mx-auto grid max-w-5xl gap-8 lg:grid-cols-[1.1fr_.9fr]"><div>{item.image ? <figure><img src={item.image.url} alt={item.image.alt} className="h-[320px] w-full rounded-[2rem] object-cover" />{item.image.credit && <figcaption className="mt-2 text-[11px] leading-5 text-[#66705E]">Imagem: {item.image.credit}{item.image.licenseUrl ? <> · <a href={item.image.licenseUrl} target="_blank" rel="noopener noreferrer" className="font-semibold underline decoration-[#B9572D]/60 underline-offset-2">{item.image.license ?? "licença"}</a></> : item.image.license ? ` · ${item.image.license}` : null}</figcaption>}</figure> : <MvpContentState kind="missing-image" title="Imagem em curadoria" description="Este espaço será preenchido somente com imagem creditada e texto alternativo revisado." />}</div><div className="rounded-[2rem] border border-[#3C482D]/14 bg-[#FFFDF6] p-6"><p className="text-[10px] font-extrabold uppercase tracking-[0.15em] text-[#566B37]">Como usar esta informação</p><h2 className="display-font mt-3 text-3xl tracking-[-0.05em]">Decida com contexto e confirme a operação.</h2><p className="mt-3 text-sm leading-6 text-[#66705E]">Este lugar integra uma curadoria pública. Horários, disponibilidade e demais condições devem ser confirmados antes da visita.</p><div className="mt-5"><SourceLine {...item.source} /></div><div className="mt-6 flex flex-wrap gap-2">{item.routeUrl && <a href={item.routeUrl} target="_blank" rel="noopener noreferrer" onClick={() => trackMvpEvent("open_route", { item: item.id, source: "pilot_place" })} className="tap inline-flex items-center gap-2 rounded-full bg-[#3C482D] px-4 py-2.5 text-sm font-extrabold text-white hover:bg-[#566B37]"><MapPinned className="h-4 w-4" /> Abrir rota</a>}{city.slug === "teresina" && item.kind === "attraction" && <Link href={`/cidades/${city.slug}?para-comer=${item.id}#para-comer`} onClick={() => trackMvpEvent("search", { scope: "food_by_anchor", anchor: item.id })} className="tap inline-flex items-center gap-2 rounded-full border border-[#B9572D]/35 px-4 py-2.5 text-sm font-extrabold text-[#803B20] hover:bg-[#F3E1CF]"><UtensilsCrossed className="h-4 w-4 text-[#B9572D]" /> Ver opções para comer perto deste ponto</Link>}{item.contactUrl && <a href={item.contactUrl} target="_blank" rel="noopener noreferrer" onClick={() => trackMvpEvent("open_external_link", { item: item.id, source: "contact" })} className="tap inline-flex items-center gap-2 rounded-full border border-[#3C482D]/18 px-4 py-2.5 text-sm font-extrabold text-[#3C482D]"><Phone className="h-4 w-4 text-[#B9572D]" /> Contato</a>}{item.externalUrl && <a href={item.externalUrl} target="_blank" rel="noopener noreferrer" onClick={() => trackMvpEvent("open_external_link", { item: item.id, source: "place_source" })} className="tap inline-flex items-center gap-2 rounded-full border border-[#3C482D]/18 px-4 py-2.5 text-sm font-extrabold text-[#3C482D]"><ExternalLink className="h-4 w-4" /> Fonte</a>}</div></div></div></section><PlaceFeedback placeTitle={item.title} /></main></div>;
}
